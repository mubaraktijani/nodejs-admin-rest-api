<%- include('../includes/header', {logo: logo}); %>

<table  cellpadding="0" cellspacing="0">
    <tr>
        <td class="content-block aligncenter">
            <h1>Hi, <%= name %>!</h1>
        </td>
    </tr>
    <tr>
        <td class="content-block">
            <p></p>
            <p>
                We're excited to have you get started. First, you need to 
                confirm your account. Just press the button below.
            </p>
        </td>
    </tr>
    <tr>
        <td class="content-block aligncenter">
            <a href="<%= token %>" class="btn-primary">Confirm Account</a>
            <br/>
        </td>
    </tr>

    <tr>
        <td>
            <hr/>
            <p class="small">
                if that didn't work, copy and past the following link in your browser:<br/>
                <a href="<%= token %>"><%= token %></a>
            </p>
        </td>
    </tr>
</table>

<%- include('../includes/footer', {sender: sender, web_domain: web_domain}); %>